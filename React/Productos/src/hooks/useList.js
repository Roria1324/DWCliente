/*
import { useContext } from "react";
import {  } from "../context/ProviderList.jsx";

const useProducts = () => {
  const context = useContext();

  if (!context) {
    throw new Error("Error: Could not retrieve the context.");
  }
  return context;
};
export default ;*/